# PIT Artifacts

Committed outputs:

- `smoke/`: validated smoke report for the milestone PIT profile
- `baseline/`: final baseline report when generated
- `history/`: ignored locally because it is run-specific cache data

Logs are stored separately in [`logs`](/Users/haidari/Desktop/M5/logs).
